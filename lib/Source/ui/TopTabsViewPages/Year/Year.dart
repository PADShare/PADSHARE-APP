import 'package:flutter/material.dart';

class YearPage extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Scaffold(
      body: new Center(
        child: new Text("Year Page"),
      ),
    );
  }
}